/**
 * Minimal Next.js config. Keep to default behavior and allow images if needed
 */

module.exports = {
  reactStrictMode: true,
  // If you decide to use next/image and remote images uncomment and update: 
  // images: { domains: ['fakestoreapi.com'] },
};
